# Research workflow service (FastAPI). Browser calls this origin for REST.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: explicit WebSocket origin for workflow live events.
# If omitted, the UI derives ws:// or wss:// from NEXT_PUBLIC_API_URL (same host/port).
# NEXT_PUBLIC_WS_URL=ws://localhost:8000
